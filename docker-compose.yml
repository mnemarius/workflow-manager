# Local dev stack (M2): Postgres + engine + worker(s) + dashboard in one command.
# Scale workers with `docker compose up --scale worker=2` — each container's hostname
# becomes its worker id. Lease knobs (for failover demos): ENGINE_LEASE_DURATION,
# ENGINE_REAPER_INTERVAL.
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: workflow
      POSTGRES_USER: workflow
      POSTGRES_PASSWORD: workflow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U workflow -d workflow"]
      interval: 5s
      timeout: 5s
      retries: 10

  engine:
    build:
      context: .
      dockerfile: engine/Dockerfile
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/workflow
      SPRING_DATASOURCE_USERNAME: workflow
      SPRING_DATASOURCE_PASSWORD: workflow
      ENGINE_LEASE_DURATION: ${ENGINE_LEASE_DURATION:-30s}
      ENGINE_REAPER_INTERVAL: ${ENGINE_REAPER_INTERVAL:-5s}
    ports:
      - "8080:8080"   # REST + actuator
      - "9090:9090"   # gRPC (worker protocol)
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s

  worker:
    build:
      context: .
      dockerfile: worker-sdk/Dockerfile
    restart: unless-stopped
    environment:
      ENGINE_GRPC_TARGET: engine:9090
    depends_on:
      engine:
        condition: service_healthy

  dashboard:
    build:
      context: ./dashboard
    ports:
      - "5173:80"
    depends_on:
      - engine

volumes:
  pgdata:
